# 安全与信任边界

## 医疗边界

TraceGraph 是知识检索和学习系统，不是医学诊断或治疗决策工具。胸痛、呼吸困难、意识障碍、卒中征象、严重出血、严重过敏和自伤风险会在模型调用前触发确定性升级提示。

## 模型边界

- 默认不连接外部模型：默认生成器是离线摘录，不需要任何配置；
- 模型密钥只从环境变量读取，不写入源码、文档、日志或任何接口响应；
- 模型清单文件（`config/models.local.json`）**只保存 API Key 对应的环境变量名称**，密钥本身只存在于 `.env` 或系统环境变量；该清单文件已被 `.gitignore` 忽略；
- 配置缺失在**启动时**失败，报错只列出缺失的变量名，不回显变量取值；
- `/system` 不返回 `BASE_URL`：自建网关常把凭证写在地址上，因此连地址也不透出；
- **浏览器永远拿不到模型网关凭证**：不返回 API Key、不返回 `Authorization` 头、不返回完整网关地址、不返回带敏感参数的 URL。`GET /models` 每项只有 `id` / `label` / `model` / `available` / `kind`（不可用时多一个 `reason`）；
- **浏览器不能提交 `base_url` 或 `api_key`**：`/query` 只接受 `generator_id`，且必须是服务端清单里已有的 ID。允许提交地址等于把服务端变成任意地址的转发器（SSRF），允许提交密钥等于让前端把凭证写进服务端请求；
- **模型选择是请求级参数，不是可修改的进程全局状态**：一次请求的选择不会影响并发请求或后续请求，也就不存在「改一次配置影响所有人」的越权面。

### 模型输出的约束

- 生成器的产出**只有结构化主张**（`GeneratedAnswer.claims`），没有正文。`answer.text` 由后端从通过校验的主张确定性拼装，模型返回的自由文本不会被读取，因此正文里不可能出现任何未被证据支持的内容；
- 图路径完全由后端生成，不传给模型，模型也无从自行编造路径；
- 发送给模型的内容限于当前问题和已经召回的**原文事实**证据；多跳推导关联不经过模型；
- DUTMed 原文在提示词中被标注为不可信数据，其中的指令一律忽略；
- 模型返回的每个引用 ID 都必须属于输入 Evidence；`claims` 为空、引用了未提供的 ID、响应不是合法 JSON，任何一项都会让整条回答作废，不返回部分通过的结果；
- 模型失败时返回结构化 `system_error` 和机器可读的 `error_code`，不向用户暴露堆栈、上游地址或密钥；
- **降级默认不发生**：只有显式设置 `TRACEGRAPH_LLM_FALLBACK=extractive` 才在使用模型失败后改用离线摘录，且降级事实会写进 `/system`、`metrics` 和回答的 `warnings`。

## 前端边界

- 模型与其他接口返回的文本一律作为纯文本渲染，**不使用 `dangerouslySetInnerHTML`，也不使用 `innerHTML`**，因此模型输出无法注入标记；
- 前端不读取、不存储任何密钥：`/system` 与 `/models` 都不提供，页面也就无从展示；
- **页面上没有 API Key 输入框，也不显示模型网关地址**。密钥一旦进入浏览器，就会经过网络下发、落在浏览器内存与开发者工具里、随构建产物一起分发，任何能打开页面的人都能拿走它——而它代表的是服务端进程持有的凭证；
- 模型选择器只把 `GET /models` 返回的 ID 回传给 `/query`，不构造任何地址或凭证；
- 后端 HTTP 500 只回一句固定文案，真实异常堆栈留在服务端日志。

## 上传边界

- 大小上限默认 10 MiB（`TRACEGRAPH_MAX_UPLOAD_MB`），超限返回 HTTP 413 与 `file_too_large`；
- **前端先查一次，后端在解码后按真实字节数再查一次**。中间件还会按 `Content-Length` 做一次粗筛。前端那次只是为了省掉一次注定失败的往返，从不被信任；
- 扩展名白名单在**后端**判定（TXT / Markdown / JSON / JSONL / CSV / PDF），不在清单里的返回 400 `unsupported_document`；
- **文件名只作为来源名称保存**，`_source_name()` 先剥掉路径再入库；它不参与服务器文件路径拼接，上传内容也不写入任意磁盘位置，切片与原文进的是 SQLite；
- PDF 只提取内嵌文本层，**不做 OCR**：扫描件没有文本层，会得到明确提示，而不是一个空文档；
- 本次不支持 DOCX、图片和 Excel；
- 上传的文档**不会生成任何 Neo4j 实体关系**，因此不会影响图检索与多跳推导的结果——图谱只能由 schema 明确的 DUTMed 专用导入写入。

## 数据边界

- 聚合指标不保存问题和文档正文；
- SQLite 数据默认位于被 Git 忽略的 `data/local/`；
- `.env`、`config/models.local.json`、`frontend/node_modules/` 和 `frontend/dist/` 均被 `.gitignore` 忽略，`.env.example` 与 `config/models.example.json` 内只有空值与占位地址；
- 删除文档会同步撤销关系证据；
- 当前版本没有身份认证、多租户隔离和字段级权限，因此只适合本地或受控网络。

## 部署前必须补充

公网部署前需要单独加入 TLS、身份认证、权限、速率限制、恶意文件扫描、审计日志、备份和敏感数据保留策略。

上传大小限制已经有了基础版本（默认 10 MiB、扩展名白名单、前后端双闸门），但它只防误操作与明显的滥用，**不是对抗性防护**：中间件虽然只看 `Content-Length`、不读请求体，但 handler 仍会把整份内容解码进内存，因此限速、按用户的配额和恶意文件扫描都还需要单独补上。
