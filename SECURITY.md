# 安全与信任边界

TraceGraph 当前定位为本机或受控网络中的知识库原型，不是可直接暴露到公网的多用户服务。

## 模型连接与密钥

- 默认模型是离线摘录，不连接任何外部服务，也不需要 API Key。
- 用户可以在前端“模型设置”中填写服务地址和 API Key。浏览器会把这些信息提交给本机后端，因此密钥会短暂存在于当前页面内存和环回 HTTP 请求中。
- 模型连接管理接口只接受环回客户端访问。它们不应通过反向代理转发给公网用户。
- 保存的连接写入 `data/local/model-connections.json`。该文件包含**明文 API Key**，已被 Git 忽略，但没有加密；能够读取这台电脑上该文件的人，也能读取密钥。
- 模型发现可以使用一次性凭证：请求结束后不落盘。保存连接时才会把密钥写入本机连接文件。
- 管理接口和普通模型列表不会回显 API Key，只返回 `has_api_key` 等安全元数据。`/system` 和 `/models` 也不会返回模型网关地址。
- 本机连接列表会把已保存的 `base_url` 返回给环回前端，方便用户维护连接；不要把该管理接口开放给不受信任的客户端。
- `.env` 和 `config/models.local.json` 是另一种本机配置方式，均已被 Git 忽略。不要把真实密钥写入源码、示例配置或提交记录。
- 连接对象的调试输出会隐藏 API Key，错误响应也不会回显密钥、授权头或上游响应正文。

## 模型输出与证据

- 发送给回答模型的内容限于当前问题和已经召回的原文证据；文档内容始终按不可信输入处理。
- 模型返回结构化主张和引用 ID。引用必须属于本次输入的 Evidence，否则整次模型结果无效。
- 多跳图路径由后端生成，不由模型自由编造，也不会被当作当前实体的直接原文结论。
- 模型失败默认不会静默切换。只有显式设置 `TRACEGRAPH_LLM_FALLBACK=extractive` 才允许降级，并在系统状态和回答警告中标明。
- 页面把模型输出作为纯文本渲染，不使用 `dangerouslySetInnerHTML` 或 `innerHTML`。

## 文档上传与原件

- 支持 TXT、Markdown、JSON、JSONL、CSV 和带文本层的 PDF；不支持的扩展名由后端拒绝。
- 单次上传默认上限为 10 MiB。前端会预检，后端仍按实际字节数重新校验。
- 文件名只作为来源名称，不直接参与服务器路径拼接。
- 上传成功后，文档原始字节保存在 `data/workspaces/{workspace_id}/documents/...` 的受控目录中；文档版本、切片、哈希和原件元数据保存在 SQLite。
- 原件路径由服务端生成并校验不能逃逸存储根目录。删除文档时会清理它自己的原件和相关证据。
- PDF 只提取内嵌文本层，不执行 OCR、图片识别或恶意文件扫描。

## 知识抽取与图谱发布

- 新文档进入文本知识库后可以立即参与关键词检索，但不会自动把模型输出写入图谱。
- 抽取结果先进入候选区，并受当前知识库允许的实体类型、关系类型和 Evidence ID 约束。
- 候选需要人工批准并显式发布，之后才会写入 SQLite 或 Neo4j 图后端，参与图检索和多跳路径。
- 结构化 DUTMed JSONL 可以通过专用导入器确定性映射到图谱，不经过自由文本候选审核流程。
- 删除文档会撤销对应的关系证据；失去全部证据的关系会被删除。

## 本地数据与隐私

- SQLite 数据库和模型连接默认位于 `data/local/`。
- 用户上传的原件位于 `data/workspaces/`。
- `.env`、本机模型配置、运行数据库、上传原件、日志、虚拟环境、前端依赖和构建结果均被 `.gitignore` 排除。
- 聚合指标不保存问题正文和文档正文。
- Workspace 只提供逻辑数据隔离，不是用户权限边界。当前没有登录、角色、租户授权或字段级权限控制。
- 内置医疗适配器包含急症升级提示，但 TraceGraph 不是诊断或治疗决策系统。

## 公网部署前必须补充

当前服务只应绑定环回地址或运行在受控网络。公网部署前至少需要增加：

- TLS、身份认证和按 Workspace 的授权；
- 模型连接管理接口的管理员权限；
- 速率限制、用户配额和请求体流式限制；
- 恶意文件扫描、内容安全策略和审计日志；
- API Key 的加密存储或外部密钥管理服务；
- 数据备份、删除策略和敏感数据保留策略；
- 反向代理配置审计，确保环回限制不会被代理拓扑绕过。

如果发现安全问题，请不要在公开 Issue 中附带密钥、真实文档或数据库文件。
